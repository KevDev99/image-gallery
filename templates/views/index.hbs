<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/bootstrap.min.css">
    <title>Image Search Gallery</title>
</head>
<body>
    {{>header}}
    
    <main>
        <section id="sectionImages">
            <div class="container-fluid">
                <div class="row">
                    {{#each images}}
                        <a class="col-sm-6 col-md-4 col-lg-3 mb-3 unsplash-img-container" href="{{this.links.download}}">
                            <img class="unsplash-img" src="{{this.urls.regular}}" alt="unsplash" />
                        </a>
                    {{else}}
                        <p class="empty">No content</p>
                    {{/each}}
                </div>
            </div>
        </section>
    </main>
       
    {{>scripts}}
</body>
</html>